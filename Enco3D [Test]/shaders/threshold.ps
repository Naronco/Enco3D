#version 120

varying vec3 texCoord;

uniform sampler2D thresholdTexture;
uniform float threshold;

void main()
{
	vec4 color = texture2D(thresholdTexture, texCoord.xy);
	gl_FragColor = clamp((color - threshold) / (1.0f - threshold), 0.0f, 1.0f);
}